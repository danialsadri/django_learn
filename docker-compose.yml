version: "3.9"

services:
  backend:
    container_name: backend
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./core:/app
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY = 'django-insecure-+-f7-rxa3g0x$_^g00mk$$_)je=tl%33zchzre^a)caete^*ji'
      - DEBUG = True
      - ALLOWED_HOSTS = *
